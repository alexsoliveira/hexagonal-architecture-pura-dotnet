# Copie este arquivo para .env e preencha com suas credenciais
AZURE_DEVOPS_PAT=chave_de_acesso_pessoal_aqui
AZURE_DEVOPS_ORG=organizacao_azure_devops_aqui
AZURE_DEVOPS_PROJECT=

# MCP Server Configuration
MCP_TRANSPORT=tipo_de_transporte_aqui
ASPNETCORE_ENVIRONMENT=ambiente_de_execucao_aqui

# ──────────────────────────────────────────────────────────────
# Docker Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to .env.docker and adjust as needed for your environment
# ──────────────────────────────────────────────────────────────

# Database Configuration
DB_NAME=HexagonalLab
DB_PORT=1433
DB_PASSWORD=HexagonalLab@2024!
DB_USER=sa

# API Configuration
API_PORT=5000
ASPNETCORE_ENVIRONMENT=Development

# Worker Configuration
# (Uses same database connection string)